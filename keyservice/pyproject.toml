[project]
name = "rdst-keyservice"
version = "0.1.0"
description = "RDST Key Service - trial credits proxy for Anthropic API"
requires-python = ">=3.12"
dependencies = [
    "webtypy>=0.1.7",
    "fastapi",
]

[dependency-groups]
dev = [
    "workers-py",
    "workers-runtime-sdk",
]
